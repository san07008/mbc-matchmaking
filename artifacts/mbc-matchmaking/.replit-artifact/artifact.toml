kind = "web"
previewPath = "/"
title = "MBC Matchmaking"
version = "1.0.0"
id = "artifacts/mbc-matchmaking"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20981

[services.development]
run = "pnpm --filter @workspace/mbc-matchmaking run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/mbc-matchmaking", "run", "build" ]
publicDir = "artifacts/mbc-matchmaking/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20981"
BASE_PATH = "/"
